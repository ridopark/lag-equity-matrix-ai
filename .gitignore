# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local env. .env.example is the committed template; every other variant is local.
.env*
!.env.example
.pytest_cache/
.ruff_cache/
.mypy_cache/

# experiment data (not committed) -- data/* not data/, so the negation below
# is consulted; git never descends into an excluded *directory*.
data/*
# ...except the exclusion list runner.py loads at startup: a point-in-time
# Alpaca snapshot of leveraged/inverse product *names*, carrying no signal or
# price content, which re-running build_exclusions.py does not reproduce.
!data/excluded-etfs.csv

# Data by kind, not only by location. Everything above is path-based, so a bar
# pull, an extract or a checkpoint written anywhere else would not be caught.
# Nothing of these types is meant to be tracked; add an explicit negation if
# that ever changes, rather than loosening these.
*.parquet
*.sqlite
*.sqlite3

# ...except the synthetic universe: fabricated prices for fabricated tickers,
# generated by scripts/make_synthetic.py. It carries no market or signal content
# and is what makes check_baseline.py --synthetic runnable from a clone (D-52).
!tests/fixtures/synthetic-closes.parquet

# playwright-mcp scratch output (console dumps, snapshots, screenshots)
.playwright-mcp/
# screenshots land in the repo root when the tool is given a bare filename
/*.png

# unlazy local coordination state (per-machine, not shared)
.claude/settings.local.json
.unlazy/
.unlazy-hook-state.json


# ArangoDB credentials live outside the repo (~/.lagmatrix-arango-pw)

# stray scratch file, not part of the project
src/hello.py

# Deliberately unpublished: this plan maps the homelab cluster in detail --
# host, namespaces, Secret and PVC names -- and states plainly which box runs
# real-money trading and where authentication is absent. This repo is public.
# Kept locally, held out of history on purpose (2026-09-09). Removing this line
# republishes it.
docs/plans/PLAN-2026-09-09-homelab-deploy.md
