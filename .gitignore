# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local env
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/

# experiment data (not committed) -- data/* not data/, so the negation below
# is consulted; git never descends into an excluded *directory*.
data/*
# ...except the golden file scripts/check_baseline.py diffs against, and the
# exclusion list runner.py loads at startup (a point-in-time Alpaca snapshot,
# so re-running build_exclusions.py does not reproduce it).
!data/baseline-98.csv
!data/excluded-etfs.csv

# unlazy local coordination state (per-machine, not shared)
.claude/settings.local.json
.unlazy/
.unlazy-hook-state.json

# langgraph checkpoints (local run state)
data/checkpoints.sqlite
